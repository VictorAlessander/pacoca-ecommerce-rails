<%= content_for :body do %>

	<h1>Editing Category</h1>

	<%= render 'form', category: @category %>

	<%= link_to 'Show', @category, class: "waves-effect waves-light btn" %> |
	<%= link_to 'Back', categories_path, class: "waves-effect waves-light btn" %>

<% end %>