<%= content_for :body do %>

	<h1>Editing Cart</h1>

	<%= render 'form', cart: @cart %>

	<%= link_to 'Show', @cart, class: "waves-effect waves-light btn" %> |
	<%= link_to 'Back', carts_path, class: "waves-effect waves-light btn" %>

<% end %>