<%= content_for :body do %>

  <p id="notice"><%= notice %></p>

  <h1>Carts</h1>

  <table class="table">
    <thead>
      <tr>
        <th>Cod</th>
        <th>Name</th>
        <th>Price</th>
        <th>Quantity</th>
        <th>Person</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @carts.each do |cart| %>
        <tr>
          <td><%= cart.cod %></td>
          <td><%= cart.name %></td>
          <td><%= cart.price %></td>
          <td><%= cart.quantity %></td>
          <td><%= cart.person %></td>
        </tr>
      <% end %>
    </tbody>

    <% if @total_price %>
      <tr>
        <td>TOTAL: R$ <%= @total_price %></td>
      </tr>
    <% else %>
      <tr>
        <td>TOTAL: R$ 0</td>
      </tr>
    <% end %>
  </table>

  <br>

  <%= link_to 'New Cart', new_cart_path, class: 'button is-primary' %>

<% end %>